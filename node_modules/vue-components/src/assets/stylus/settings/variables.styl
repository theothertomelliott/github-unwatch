// Importing from settings.json
$JSONVars = json('./../../../config.json', { hash: true, leave-strings: true })

if $JSONVars.element_separator
  $element_separator = $JSONVars.element_separator
else
  $element_separator = '__';

if $JSONVars.modifier_separator
  $modifier_separator = $JSONVars.modifier_separator
else
  $modifier_separator = '--';

// Rem default
$rem_default = 16px

$border_radius = .375rem